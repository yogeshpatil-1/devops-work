apiVersion: v1
kind: Service
metadata:
  name: hello-dev
  labels:
    app: hello-dev
spec:
  type: ClusterIP
  selector:
    app: hello-dev
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http